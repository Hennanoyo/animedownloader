# Repository Instructions

Follow the repository instructions in `AGENTS.md`.

For frontend work, also follow `web/AGENTS.md`.

For backend work, also follow `server/AGENTS.md`.

Architecture and design decisions are documented under `docs/architecture/` and `docs/decisions/`.

GitHub Actions is the verification layer. Do not treat a change as complete until the applicable CI checks pass.
