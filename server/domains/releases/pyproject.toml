[project]
name = "animedownloader-releases"
version = "0.1.0"
description = "AnimeDownloader release domain"
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["uv_build>=0.12.17,<0.13"]
build-backend = "uv_build"
